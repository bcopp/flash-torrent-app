/*
import React, { useState } from "react";
import { textChangeRangeIsUnchanged } from "typescript";

function useAddedTorrents() {
  const [addedTorrents, setAddedTorrents] = useState(readTorrentsFolder());

  return addedTorrents, setAddedTorrents;
}

function td(i) {
  return {
    title: query + i,
    size: i,
    seeders: i,
    leechers: i,
    uploaded: i,
    link: i,
    magnet: i
  }
};

function readTorrentsFolder() {
  let d = [td(1), td(2), td(3), td(4), td(5)];
}
*/